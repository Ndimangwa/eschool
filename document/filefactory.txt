FileFactory Services
1. promise1 = updateExistingFile(filename, doc);
2. promise1 = createBackupFile(filename)
3. promise1 = createFile(filename, rootnodename)
4. doc = readFile(filename)
5. nodeCollection = findNodes(doc, nodename, pos)
6. promise1 = deleteNodeCollections(filename, doc, collectionToDelete)
7. promise1 = addNodeAfterCollections(filename, doc, referenceCollection, nodeToAdd)
8. promise1 = addNodeBeforeCollections(filename, doc, referenceCollection, nodeToAdd)
9.promise1 = appendNode(filename, doc, referenceNodeName, pos, nodeToAppend)
10. promise1 = replaceContentOfNodeCollections($filename, $doc, $referenceCollection, $contentText)
11. promise1 = replaceNodeCollections($filename, $doc, $referenceCollection, $nodeToReplace)
12. nodeCollection = getListOfNodesWithValueFromCollection($listOfAllNodes, $pos, $value, $iscasesensitive)
13. nodeCollection = findCustomNodes($doc, $customExpression)
14. nodeCollection[0] = findParentOfNode($doc, $refnodename, $pos)
15. nodeCollection[0] = findParentOfCustomNode($doc, $customExpression)
16.checksum = calculateFileChecksum($filename)
17. checksum = calculateDOMChecksum($doc)
18. doc = createDOMChecksum($doc)
19. checksum = getFileChecksum($filename)
20. checksum = getDOMChecksum($doc)
21. boolean = isDOMIntegrityPassed($doc)
22. boolean = isFileIntegrityPassed($filename)
23. promise1 = restoreFile($filename)